---
name: "Claude Terminal (unsnow)"
description: "Claude Code CLI terminal for Home Assistant. unsnow fork of ESJavadex/claude-code-ha: musl 1.2.5 base (fixes statx), working persist-install, baked ttyd/tmux, add-on-release update model."
version: "3.0.1"
slug: "claude_terminal_unsnow"
init: false

# Supported architectures
arch:
  - aarch64
  - amd64
  - armv7

# External resources
url: "https://github.com/unsnow-iac/claude-code-ha"

# Web interface configuration
ingress: true
ingress_port: 7680
panel_icon: mdi:code-braces-box
panel_title: "Claude Terminal (unsnow)"
panel_admin: true

# Port configuration
ports:
  7680/tcp: 7680
  7681/tcp: 7681
ports_description:
  7680/tcp: "Image upload service and web interface"
  7681/tcp: "ttyd terminal (embedded in web interface)"

# Add-on configuration options
# (use_persistent_claude / auto_update_claude_on_start removed: that layer
#  targeted an obsolete cli.js path and fought the baked-binary model. Claude
#  updates are delivered by bumping CLAUDE_VERSION and rebuilding the add-on.)
options:
  auto_launch_claude: true
  dangerously_skip_permissions: false
  persistent_apk_packages: []
  persistent_pip_packages: []

schema:
  auto_launch_claude: bool?
  dangerously_skip_permissions: bool?
  persistent_apk_packages:
    - str?
  persistent_pip_packages:
    - str?

# Volume mappings for file access
map:
  - config:rw    # Access to Home Assistant configuration files

# API access for Home Assistant integration
hassio_api: true
hassio_role: manager
homeassistant_api: true
auth_api: true

# Service startup configuration
startup: services

# Web UI access URL
webui: http://[HOST]:[PORT:7680]/
